<h1 class="text-center"> Find images from hex code </h1>
<%= form_for Image.new, url: { action: "create" }, html: { class: 'col-md-6 offset-md-3'} do |f| %>
  <div class="form-group">
	  <%= f.label :color %>
	  <%= f.text_field :color, class: "form-control jscolor" %>
  </div>
  <div class="form-group">
  	<%= f.label :percentage %>
  	<%= f.text_field :percentage, class: "form-control" %>
	</div>
  <%= f.submit "Find", class: "btn btn-primary" %>
<% end %>


<hr>
<div class="text-center">
Use GitHub's <a href="https://noopschallenge.com/challenges/hexbot"> Hex Noop </a> to return a random color.
<%= button_to 'Randomize' , home_index_path(color: 0), class: "btn btn-primary" %> 
</div>